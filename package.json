{
  "name": "sitehub",
  "version": "1.0.0",
  "description": "SiteHub — every deployed site auto-detected in one place.",
  "private": true,
  "scripts": {
    "icons": "python scripts/gen-icons.py",
    "deploy": "python scripts/deploy.py"
  },
  "dependencies": {
    "@capacitor/android": "^8.0.0",
    "@capacitor/core": "^8.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.0",
    "typescript": "^7.0.2"
  }
}
