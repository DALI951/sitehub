import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.dali951.sitehub',
  appName: 'SiteHub',
  webDir: 'www',
  backgroundColor: '#0a0a0f',
  android: {
    backgroundColor: '#0a0a0f'
  }
};

export default config;